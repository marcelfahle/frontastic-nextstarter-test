import React from 'react'

import LoaderButton from './index'

export default {
    title: 'Loader Button',
}

export const loaderButton = () => {
    return (
        <div>
            <LoaderButton />
        </div>
    )
}
